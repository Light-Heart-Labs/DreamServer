# Extend default gitleaks rules; allowlist template/example files with placeholder secrets.
[extend]
useDefault = true

# Ignore documented example env files (CHANGEME, sk-dream- in comments, etc.)
[allowlist]
paths = [
  '''\.env\.example$''',
  '''\.env\.sample$'''
]
